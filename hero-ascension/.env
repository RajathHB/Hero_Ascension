# In development, API requests are proxied through Vite (see vite.config.js).
# No VITE_API_URL is needed — it defaults to /api which the proxy forwards.
#
# For production, uncomment and set the deployed backend URL:
# VITE_API_URL=https://your-backend.example.com
